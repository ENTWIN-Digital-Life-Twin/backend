jwt:
  secret: TestOnlySecretKeyNeedAtLeast32BytesLong!!

planning:
  default-timezone: Africa/Casablanca
  day-start: "08:00"
  day-end: "23:00"
  overload-threshold-ratio: 0.9

ai:
  service-url: http://127.0.0.1:9
  connect-timeout-ms: 200
  read-timeout-ms: 200

spring:
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
